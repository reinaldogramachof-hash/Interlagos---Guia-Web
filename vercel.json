{
    "buildCommand": "cd interlagos-conectado && npm install && npm run build",
    "outputDirectory": "interlagos-conectado/dist",
    "installCommand": "echo 'install handled in buildCommand'",
    "framework": null,
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}